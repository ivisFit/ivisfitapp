'use client';

import { DemoHomePage } from './RoutePreview.example';

/** Página demo standalone (sin iframe) para probar componentes editables. */
export default function DemoPage() {
  return <DemoHomePage />;
}

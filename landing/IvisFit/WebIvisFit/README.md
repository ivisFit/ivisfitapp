# LEGACY — no usar como web pública

Esta carpeta es la **landing Vite antigua** de IVIIS Fit.

La web viva (CMS + chatbot + planes) vive en:

- `apps/frontend-app` → rutas públicas `/` y `/[slug]`

No desplegar ni editar contenido de marketing acá. Conservamos el árbol solo como referencia histórica.
